# BrokerOS web

Vite + React + TypeScript frontend for BrokerOS.

```bash
npm install
npm run dev
```

Runs at http://localhost:5173 and expects the API at `VITE_API_BASE_URL` (default `http://localhost:5000`).

See the repository root README for the full local setup.
