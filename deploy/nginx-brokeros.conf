# BrokerOS Nginx Configuration
# Add this to your existing Nginx setup
#
# Option 1: Add as a new server block (for IP-based access on different port)
# Option 2: Add as location blocks in existing server (for path-based routing)
# Option 3: Create a subdomain server block
#
# Choose ONE option below and configure accordingly

# ============================================
# OPTION 1: Separate server on port 8000 (Recommended for testing)
# ============================================
# Access at: http://217.217.249.136:8000
# 
# server {
#     listen 8000;
#     listen [::]:8000;
#     server_name _;
#
#     client_max_body_size 12M;
#
#     # API routes
#     location /api {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /health {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     location /swagger {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     # Frontend (everything else)
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# ============================================
# OPTION 2: Path-based routing (/broker prefix)
# ============================================
# Access at: http://217.217.249.136/broker
# Add these location blocks to your existing server block
#
# location /broker/api {
#     rewrite ^/broker(.*)$ $1 break;
#     proxy_pass http://127.0.0.1:8080;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
#
# location /broker {
#     rewrite ^/broker(.*)$ $1 break;
#     proxy_pass http://127.0.0.1:3000;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
# }

# ============================================
# OPTION 3: Full server block for direct IP access
# ============================================
# This replaces default handling on port 80
# Only use if you want BrokerOS as the main site

server {
    listen 8000;
    listen [::]:8000;
    server_name 217.217.249.136;

    client_max_body_size 12M;
    
    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # API routes
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /health {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location /swagger {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Frontend (everything else)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
